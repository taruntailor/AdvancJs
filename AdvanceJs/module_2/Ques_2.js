// ● Is array copied?
// let fruits = ["Apples", "Pear", "Orange"]; // push a new value
// into the "copy"let shoppingCart = fruits;
// shoppingCart.push("Banana"); // what's in fruits? alert(
// fruits.length ); // ?


let fruits = ["Apples", "Pear", "Orange"];
let shoppingCart = fruits;
shoppingCart.push("Banana"); 
alert(fruits.length);

